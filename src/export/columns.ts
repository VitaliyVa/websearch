/**
 * ЖОРСТКЕ ПРАВИЛО: колонки A..W — машинні, скрипт їх перезаписує.
 * Колонки X.. — людські (продажники), скрипт їх НІКОЛИ не читає і не пише.
 * Експорт працює через діапазон A:W, тому нотатки продажників фізично
 * не можуть бути затерті навіть при повному перепрогоні.
 *
 * Межа машинної частини рахується з MACHINE_COL_COUNT, а не зашита буквою —
 * коли додали «Опис для продажника», людські колонки поїхали з W..Z на X..AA,
 * і будь-яке зашите «W» стало б тихою помилкою: статус писався б у колонку
 * опису. З тієї ж причини Apps Script шукає їх за назвою заголовка.
 */

export const MACHINE_COLUMNS = [
  'place_id',            // A
  'Назва компанії',      // B
  'Тип діяльності',      // C
  'Тир',                 // D
  'Місто / район',       // E
  'Сайт',                // F
  'Google Maps',         // G
  'Телефон',             // H
  'Email',               // I
  'Соцмережі',           // J
  'Оцінка сайту 1-10',   // K
  'Причини оцінки',      // L
  'Складність розробки', // M

  'Мовний скор',         // N
  'Мова',                // O
  'Докази мови',         // P
  'Техстек',             // Q
  'PSI моб / деск',      // R
  'Адаптивний',          // S
  'HTTPS',               // T
  'Рейтинг / відгуки',   // U
  'Скрін + PSI звіт',    // V
  'Опис для продажника', // W
] as const;

export const HUMAN_COLUMNS = [
  'Статус',              // X
  'Хто веде',            // Y
  'Дата контакту',       // Z
  'Коментар',            // AA
] as const;

export const MACHINE_COL_COUNT = MACHINE_COLUMNS.length; // 23 → A..W
export const ALL_COLUMNS = [...MACHINE_COLUMNS, ...HUMAN_COLUMNS];

/**
 * Порядок ключів = порядок вкладок у таблиці.
 *
 * «Ліди без сайту» стоять одразу після «Leads» навмисно: це теж готові до
 * дзвінка ліди, просто з іншим оффером — не переробити сайт, а зробити перший.
 * Решта NO_SITE лишається окремо як неперевірена маса.
 */
export const TABS = {
  leads: 'Leads',
  noSiteLeads: 'Ліди без сайту',
  /*
   * Мова підтверджена, але сайт сучасний — переробляти нема чого.
   * Раніше такі просто відсіювались, і це була помилка: власник свій,
   * бізнес живий, просто оффер інший — SEO, реклама, підтримка, доробки.
   */
  upsell: 'З хорошим сайтом',
  manual: 'Manual review',
  noSite: 'NO_SITE',
  rejected: 'Rejected',
  meta: 'Meta',
} as const;

export type TabKey = keyof typeof TABS;
